FILES.txt — messageEngine repository file index
================================================

BUILD / CONFIG
--------------
Makefile
  GNU make build file; compiles all libraries, server, client, and test binaries
  with the full strict warning set (-Wall -Wextra -Wpedantic -fno-exceptions -fno-rtti).
  Targets: all, server, client, tests, run_tests, lint, cppcheck, scan_build,
  static_analysis, coverage, coverage_show, clean.

docs/CMakeLists.txt
  CMake equivalent of the Makefile (provided for IDEs/CI that prefer cmake).

.gitignore
  Excludes the build/ output directory from version control.

.cppcheck-suppress
  Cppcheck suppression list; each entry has a justification comment and a
  DEFECT_LOG.md reference.

src/.clang-tidy
  Clang-Tidy configuration for production code (src/); strict rules matching
  JPL Power of 10, MISRA C++:2023, and F-Prime style subset.

tests/.clang-tidy
  Clang-Tidy configuration for test code (tests/); slightly relaxed relative
  to src/ to accommodate test-framework idioms (see CLAUDE.md §3).

PROJECT DOCS
------------
README.md
  Project overview: requirements summary, architecture diagram, directory
  structure, build instructions (required and optional tools), test execution,
  coverage analysis, static analysis, demo usage, library integration guide,
  coding standards, and code statistics.

CLAUDE.md
  Top-level project-specific requirements: layered architecture, message
  envelope spec, transport API, impairment engine spec, delivery semantics,
  per-transport backend requirements, and traceability/inspection/safety rules.

.claude/CLAUDE.md
  Global C/C++ coding standard: all 10 JPL Power of 10 rules (including the
  infrastructure-loop deviation), MISRA C++:2023, F-Prime style subset,
  architecture layering rules, NEVER_COMPILED_OUT_ASSERT policy, and
  instructions for AI-assisted development.

.claude/settings.local.json
  Claude Code local settings (editor preferences, tool permissions).

docs/DirectoryStructure.txt
  High-level directory tree showing all source, test, build, and docs
  directories.

docs/FILES.txt
  This file. Per-file descriptions for every source, config, and doc artifact.

docs/Design.md
  Comprehensive software design document: design goals and principles, system
  context, layered architecture, full MessageEnvelope data model, error
  handling model, memory model (all static allocation, 9-frame worst-case
  stack), concurrency model (SPSC RingBuffer, std::atomic rationale), module
  designs for all core and platform components, state machines (AckTracker,
  RetryManager, ImpairmentEngine partition and delay buffer), wire format
  (byte-level layout), impairment pipeline, safety design (hazard table and SC
  functions), testability design, capacity constants table, and 6 key design
  decisions with rationale.

docs/ClassArchitecture.md
  Class relationship overview: inheritance, composition, and dependency graph.

docs/Requirements.txt
  Numbered application requirements ([REQ-x.x]) in flat-text form; source of
  truth for traceability.

docs/TRACEABILITY_MATRIX.md
  Cross-reference table: requirements → implementing functions → verifying
  tests. Updated after every change.

docs/HAZARD_ANALYSIS.md
  Software Safety Hazard Analysis and FMEA: hazard IDs (HAZ-001 … HAZ-007),
  failure modes, effects, detection mechanisms, and safety-critical (SC)
  function classification.

docs/STATE_MACHINES.md
  Formal state machine specifications for AckTracker (FREE / PENDING / ACKED),
  RetryManager (INACTIVE / WAITING / DUE / EXHAUSTED / EXPIRED), and
  ImpairmentEngine (NO_PARTITION / PARTITION_ACTIVE, plus delay-buffer slot
  states SLOT_FREE / SLOT_HELD / SLOT_READY).

docs/STACK_ANALYSIS.md
  Worst-case stack depth analysis: identifies the deepest call chains
  (outbound send, inbound receive, retry pump, DTLS send/receive) and the
  maximum stack frame for each function; confirms the 10-frame worst-case
  ceiling (~764 B peak, Chain 5 — DTLS outbound send).

docs/WCET_ANALYSIS.md
  Worst-Case Execution Time (WCET) analysis: per-function operation counts
  based on compile-time capacity constants, with O() classification and
  notes on bounded vs unbounded behavior.

docs/INSPECTION_CHECKLIST.md
  Code inspection checklist used for formal peer reviews: entry gate, Power of
  10 rules, MISRA/F-Prime items, safety requirements, and sign-off fields.

docs/DEFECT_LOG.md
  Defect and inspection log; records all formal code inspections (INSP-xxx),
  deviations, and cppcheck suppressions with justification and resolution.

docs/use_cases/use_case_format.txt
  15-section flow-of-control document template used to generate individual
  UC_*.md documents; defines the quality bar and output format requirements.

docs/use_cases/HIGH_LEVEL_USE_CASES.md
  Index of 20 high-level use cases (HL-1 through HL-20) with links to the 42
  detailed Flow-of-Control documents.

docs/use_cases/UC_01 … UC_42
  42 Flow-of-Control documents, one per use case. Each document has 15
  sections: overview, entry points, step-by-step control flow, call tree, key
  components, branching logic, concurrency, memory/ownership, error handling,
  external interactions, state changes, sequence diagram (ASCII), init vs
  runtime, known risks, and unknowns/assumptions.

SOURCE — src/core/   (message layer; no OS or socket dependencies)
-------------------------------------------------------------------
Types.hpp
  All shared enumerations (MessageType, ReliabilityClass, Result, Severity,
  TransportKind, OrderingMode), compile-time capacity constants
  (MSG_MAX_PAYLOAD_BYTES=4096, MSG_RING_CAPACITY=64, ACK_TRACKER_CAPACITY=32,
  DEDUP_WINDOW_SIZE=128, MAX_RETRY_COUNT=5, IMPAIR_DELAY_BUF_SIZE=32, etc.),
  and the NodeId typedef.

Assert.hpp
  NEVER_COMPILED_OUT_ASSERT macro: always compiled in (never NDEBUG-disabled);
  debug/test builds call abort(); production logs FATAL and triggers a
  controlled component reset.

Logger.hpp
  Header-only, heap-free logger; writes severity-tagged lines to stderr using
  a fixed 512-byte stack buffer. No dynamic allocation.

MessageEnvelope.hpp
  The standard wire envelope struct (message_type, message_id, timestamp_us,
  expiry_time_us, source_id, destination_id, priority, reliability_class,
  payload[4096]) plus inline helpers: envelope_init, envelope_copy,
  envelope_valid, envelope_is_data, envelope_is_control, envelope_make_ack.

ChannelConfig.hpp
  ChannelConfig and TransportConfig POD structs with per-channel priority,
  reliability mode, queue depth, timeouts, and impairment toggle.
  Provides channel_config_default() and transport_config_default() inline
  helpers.

TransportInterface.hpp
  Pure-virtual abstract base class (init / send_message / receive_message /
  close / is_open) that decouples the message layer from all socket backends.

RingBuffer.hpp
  Header-only, statically allocated, lock-free SPSC ring buffer for
  MessageEnvelope (capacity MSG_RING_CAPACITY=64); push / pop / peek with
  ERR_FULL / ERR_EMPTY result codes; uses std::atomic<uint32_t> with
  acquire/release ordering.

Serializer.hpp / Serializer.cpp
  Deterministic, big-endian (network byte order) wire serialization of
  MessageEnvelope into a fixed 44-byte header + variable payload; uses manual
  bit-shift I/O (no htons/ntohl). Validates padding fields and payload bounds
  on deserialization.

MessageId.hpp / MessageId.cpp
  Monotonically incrementing 64-bit message-ID generator (MessageIdGen) with
  optional seed support; never returns 0 (reserved as invalid).

Timestamp.hpp / Timestamp.cpp
  Helpers using CLOCK_MONOTONIC: timestamp_now_us() returns current time in
  microseconds; timestamp_expired() checks whether a deadline has passed;
  timestamp_deadline_us() computes a future deadline given an offset.

DuplicateFilter.hpp / DuplicateFilter.cpp
  128-entry sliding-window dedup keyed on (source_id, message_id) pairs;
  evicts the oldest entry on overflow. Used by DeliveryEngine on the inbound
  path for RELIABLE_RETRY messages.

AckTracker.hpp / AckTracker.cpp
  Fixed-capacity (32-slot) table tracking outstanding reliable messages;
  records ACK deadlines, detects timeouts via sweep_expired(), and removes
  entries on ACK receipt via on_ack(). States: FREE / PENDING / ACKED.

RetryManager.hpp / RetryManager.cpp
  Retry scheduler with per-message exponential backoff (initial interval
  doubles each attempt, capped at 60 s); integrates with AckTracker to cancel
  retries on ACK via on_ack(); collect_due() returns messages ready for
  re-send. States: INACTIVE / WAITING / DUE / EXHAUSTED / EXPIRED.

DeliveryEngine.hpp / DeliveryEngine.cpp
  Per-channel coordinator that ties transport, ACK tracking, retry, and dedup
  together. Assigns message IDs, applies reliability semantics (BEST_EFFORT /
  RELIABLE_ACK / RELIABLE_RETRY), drives retries via pump_retries(), and
  sweeps ACK timeouts via sweep_ack_timeouts().

SOURCE — src/platform/   (OS/socket layer; adapters only, no app policy)
-------------------------------------------------------------------------
PrngEngine.hpp / PrngEngine.cpp
  Seedable xorshift64 PRNG used for deterministic impairment decisions;
  exposes next() (uint64_t), next_double() [0.0, 1.0), and next_range(lo, hi).

ImpairmentConfig.hpp
  POD configuration struct for all impairment parameters: enabled flag, fixed
  latency, jitter mean/variance, loss probability, duplication probability,
  reordering window size, partition gap/duration, and PRNG seed.
  Provides impairment_config_default() inline helper.

ImpairmentEngine.hpp / ImpairmentEngine.cpp
  Applies configurable network impairments to messages on the outbound path:
  packet loss (drop), duplication (extra copy queued), fixed latency and
  jitter (delay buffer, 32 slots), reordering (window-based release), and
  link partitions (state machine: NO_PARTITION / PARTITION_ACTIVE).
  process_inbound() handles reordering on the receive path. Fully
  reproducible given the same PRNG seed.

SocketUtils.hpp / SocketUtils.cpp
  POSIX socket primitives: socket creation, bind, connect-with-timeout,
  send_all (retry on EINTR/short write), recv_exact, sendto/recvfrom wrappers,
  and length-prefixed TCP framing helpers (write_framed / read_framed).

ISocketOps.hpp
  Pure-virtual injectable interface for all POSIX socket operations used by
  TcpBackend and UdpBackend (create_tcp, create_udp, do_bind, do_listen,
  do_accept, send_frame, recv_frame, send_to, recv_from, etc.).
  Enables fault-injection testing without modifying production code.

SocketOpsImpl.hpp / SocketOpsImpl.cpp
  Singleton implementation of ISocketOps that delegates every call to the
  real POSIX socket API. Used by production TcpBackend and UdpBackend
  instances; swapped for MockSocketOps in tests.

TcpBackend.hpp / TcpBackend.cpp
  TransportInterface over TCP; server mode supports up to 8 simultaneous
  clients via a dedicated accept thread and per-connection receive threads;
  client mode connects with timeout. Uses length-prefixed framing,
  ImpairmentEngine, and a RingBuffer inbound queue.

UdpBackend.hpp / UdpBackend.cpp
  TransportInterface over UDP datagrams; binds a single socket, serializes
  each MessageEnvelope to the wire format, and applies ImpairmentEngine before
  send. Receive path deserializes from raw datagrams.

LocalSimHarness.hpp / LocalSimHarness.cpp
  In-process simulation transport; two instances are linked via link(). Messages
  sent from one are injected directly into the peer's RingBuffer via inject()
  after passing through the ImpairmentEngine — no sockets or threads involved.
  inject() is also public so tests can push envelopes directly into the receive
  queue bypassing the impairment engine entirely (UC_42).

TlsTcpBackend.hpp / TlsTcpBackend.cpp
  Drop-in TransportInterface replacement for TcpBackend that adds optional
  mbedTLS 4.0 TLS encryption (STREAM transport). When tls_enabled=false,
  operates as plaintext TCP identical to TcpBackend (REQ-6.3.4 swappable).
  When tls_enabled=true: loads PEM cert/key via PSA Crypto, performs TLS 1.2+
  handshake on each accepted or initiated connection, and framing is preserved
  via tls_send_frame/tls_recv_frame. Supports per-client ssl_context array
  (up to MAX_TCP_CONNECTIONS=8). SC functions: send_message (HAZ-005,006),
  receive_message (HAZ-004,005).

DtlsUdpBackend.hpp / DtlsUdpBackend.cpp
  TransportInterface over DTLS-encrypted UDP (DATAGRAM transport, REQ-6.4.1).
  Server: binds UDP socket, waits for first client datagram, performs DTLS
  cookie anti-replay exchange (REQ-6.4.2), and completes handshake in init().
  Client: connects UDP socket to peer and performs handshake in init().
  MTU enforcement: rejects serialised messages exceeding DTLS_MAX_DATAGRAM_BYTES
  (1400) before encryption (REQ-6.4.4). DTLS retransmission timer via
  mbedtls_timing_delay_context (REQ-6.4.3). Plaintext UDP fallback when
  tls_enabled=false (REQ-6.4.5). SC functions: send_message (HAZ-005,006),
  receive_message (HAZ-004,005).

IMbedtlsOps.hpp
  Pure-virtual injectable interface for all mbedTLS library calls used by
  DtlsUdpBackend (crypto_init, ssl_config_defaults, x509_crt_parse_file,
  pk_parse_keyfile, ssl_conf_own_cert, ssl_cookie_setup, ssl_setup,
  ssl_handshake, ssl_read, ssl_write, ssl_set_client_transport_id, etc.).
  Enables M5 fault-injection testing of DTLS error paths without real
  certificates or network. SC functions: ssl_handshake (HAZ-004,005,006),
  ssl_write (HAZ-005,006), ssl_read (HAZ-004,005).

MbedtlsOpsImpl.hpp / MbedtlsOpsImpl.cpp
  Singleton implementation of IMbedtlsOps that delegates every call to the
  real mbedTLS 4.0 PSA Crypto library. Used by production DtlsUdpBackend
  instances; swapped for mock in tests.

ImpairmentConfigLoader.hpp / ImpairmentConfigLoader.cpp
  Free function impairment_config_load(path, cfg): reads an INI-style
  key=value text file (at most MAX_CONFIG_LINES=64 lines, 128 bytes/line)
  and populates an ImpairmentConfig struct. Always initialises cfg to safe
  defaults before parsing; probability fields clamped to [0.0, 1.0];
  unknown keys logged at WARNING_LO and ignored; returns ERR_IO only on
  fopen() failure. Implements REQ-5.2.1. SC: HAZ-002, HAZ-007.

SOURCE — src/app/   (application orchestration)
------------------------------------------------
Server.cpp
  Demo TCP echo server: binds on a configured port, receives DATA messages,
  logs them, sends echo replies, and pumps retries/ACK-timeouts each
  iteration. Runs until terminated.

Client.cpp
  Demo TCP client: connects to the server, sends five RELIABLE_RETRY messages
  with "Hello from client #N" payloads, waits for echo replies (up to 20
  bounded receive attempts per message), and reports the final send/echo count.

TESTS — tests/
--------------
test_MessageEnvelope.cpp
  5 tests: envelope_init, envelope_valid, envelope_copy, envelope_make_ack,
  and envelope_is_data / envelope_is_control.

test_Serializer.cpp
  12 tests: basic round-trip, undersized buffer, invalid envelope, truncated
  input, maximum-payload pattern fill, wire-header-size constant, zero-payload
  round-trip, header-too-short, bad padding1, bad padding2, oversized payload
  field, and zero-payload deserialization.

test_DuplicateFilter.cpp
  5 tests: basic dedup, different-source pass-through, different-ID
  pass-through, unseen message pass-through, and sliding-window wraparound /
  oldest-entry eviction.

test_AckTracker.cpp
  10 tests: init, track_ok, track_full (ERR_FULL), on_ack_ok, on_ack_wrong_src,
  on_ack_wrong_id, sweep_expired_pending, sweep_pending_not_expired,
  sweep_releases_acked (slot recycled for new message), and sweep_buf_capacity.

test_RetryManager.cpp
  12 tests: init, schedule_ok, schedule_full (ERR_FULL), on_ack_ok,
  on_ack_not_found, collect_due_immediate, collect_not_due, collect_expired,
  collect_exhausted (max retries reached), backoff_doubles (interval doubles
  each attempt), backoff_cap (interval capped at MAX_BACKOFF_MS), and
  collect_never_expires.

test_DeliveryEngine.cpp
  19 tests: send_best_effort, send_reliable_ack, send_reliable_retry,
  receive_data_best_effort, receive_expired, receive_ack_cancels_retry,
  receive_duplicate, receive_timeout, pump_retries_no_retries,
  pump_retries_fires, sweep_no_timeouts, sweep_detects_timeout,
  send_ack_tracker_full, send_retry_manager_full, pump_retries_send_fails,
  send_transport_queue_full, receive_nak_control, receive_heartbeat_control,
  and pump_retries_resends.

test_ImpairmentEngine.cpp
  12 tests: disabled pass-through, deterministic 100% loss, zero loss,
  fixed-latency gating (collect_deliverable timing), PRNG reproducibility,
  jitter enable path, duplication (1.0 probability yields two copies),
  partition state machine (init → start → active-drop → end), process_inbound
  passthrough (reorder disabled), process_inbound reorder window (window fills
  then releases), delay-buffer full with impairments enabled (ERR_FULL), and
  delay-buffer full with impairments disabled (ERR_FULL).

test_LocalSim.cpp
  4 tests: basic send/receive round-trip, bidirectional messaging (both
  directions simultaneously), queue-full overflow (ERR_FULL when ring buffer
  capacity is exceeded), and receive timeout on empty queue (ERR_TIMEOUT).

test_AssertState.cpp
  8 tests: get_handler_initially_null, set_and_get_handler,
  trigger_dispatches_to_handler, trigger_sets_fatal_flag,
  abort_handler_is_ireset_handler, check_and_clear_false,
  check_and_clear_true, and check_and_clear_idempotent.

test_TcpBackend.cpp
  25 tests covering the TcpBackend via MockSocketOps injection: config
  defaults, server bind/close, client connect failure, receive timeout, send
  with no clients, close-before-init, is_open lifecycle, loopback roundtrip,
  large-timeout cap, bad bind IP, num_channels_zero, garbage-frame
  deserialize failure, two-client compaction, client detects server close,
  and 7 mock-injection fault tests (socket create, reuseaddr, listen,
  nonblocking, impairment delay paths, recv queue overflow, send_frame fail,
  loss impairment drop).

test_UdpBackend.cpp
  18 tests covering the UdpBackend via MockSocketOps injection: config
  defaults, bind/close, receive timeout, loopback send/receive, multiple
  messages, close-before-init, is_open lifecycle, unreachable peer, bad
  bind IP, num_channels_zero, garbage datagram, and 5 mock-injection fault
  tests (create fail, reuseaddr fail, impairment delay paths, send loss
  impairment, send_to fail, recv_from fail, recv queue initial pop).

test_SocketUtils.cpp
  17 tests covering raw POSIX helpers in SocketUtils: create_tcp,
  create_udp, set_nonblocking, set_reuseaddr, bind_valid, bind_bad_ip,
  listen, accept, close, connect_with_timeout, tcp_frame_round_trip,
  tcp_recv_oversized_frame, send_to_bad_ip, udp_send_recv_round_trip,
  udp_recv_timeout, tcp_send_recv_exact, and tcp_frame_zero_payload.

MockSocketOps.hpp
  Test-only mock implementation of ISocketOps used by TcpBackend,
  UdpBackend, and TlsTcpBackend tests to inject POSIX socket failures
  (socket create, bind, listen, accept, send, recv) without real network I/O.

test_TlsTcpBackend.cpp
  30 tests covering TlsTcpBackend via ISocketOps injection: TlsConfig
  defaults, transport_config includes TLS fields, server bind plaintext,
  bad cert/key path, server bind TLS, plaintext loopback, TLS loopback,
  connect bad host, receive timeout, send no clients, echo loopback
  plaintext, post-echo remove_client, TLS verify-peer with/without CA,
  num_channels_zero, client close long timeout, server bind port zero,
  raw socket no TLS handshake, garbage frame deserialize fail, two-client
  queue prefill, two-client compact loop, bad CA file, bind port in use,
  server send before client connects, max_connections_exceeded,
  TLS zero-length frame, DI constructor, and recv queue overflow.

test_DtlsUdpBackend.cpp
  37 tests covering DtlsUdpBackend via IMbedtlsOps/ISocketOps injection:
  plaintext server bind, DTLS bad cert, plaintext loopback, DTLS loopback,
  oversized payload rejected (ERR_INVALID), receive timeout, bad key path,
  bad CA cert file, server init timeout (ERR_TIMEOUT), client bad peer IP,
  bad bind IP, server recv after client close, plaintext recv garbage
  datagram, DTLS server handshake garbage, plaintext send bad peer IP,
  receive long timeout clamp, DTLS server verify-peer without CA, valid CA
  cert load, server zero connect timeout, and 18 IMbedtlsOps mock-injection
  fault tests (crypto_init fail, ssl_config_defaults fail, ssl_conf_own_cert
  fail, ssl_cookie_setup fail, server recvfrom_peek fail, server net_connect
  fail, server ssl_setup fail, server ssl_set_transport_id fail, client
  net_connect fail, client ssl_setup fail, and more).

test_ImpairmentConfigLoader.cpp
  19 tests: file not found (ERR_IO), empty file, all fields parsed, partial
  file retains defaults, comment and blank lines skipped, whitespace around
  equals, malformed line skipped, unknown key logged, loss_probability
  clamp (>1.0 → 1.0), duplication_probability clamp, reorder_window_size
  clamp (>32 → 32), large prng_seed, negative duplication probability
  clamp (<0.0 → 0.0), leading space, leading tab, carriage return in line,
  truly malformed line, over-64-lines truncation, and malformed values.
