/**
 * @file MessageId.cpp
 * @brief Compilation unit for MessageIdGen (header-only inline implementation).
 *
 * This file exists to satisfy build system requirements.
 * The actual implementation is in MessageId.hpp (inline functions).
 *
 * Power of 10: all code is inline in the header; no external compilation unit needed.
 *
 * Implements: REQ-3.2.1
 */

#include "MessageId.hpp"

// No implementation required; all logic is in MessageId.hpp
